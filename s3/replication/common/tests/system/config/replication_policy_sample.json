{
    "Role": "arn:aws:iam::ROLE_ID:role/service-role/s3crr_role_for_REPLICATION_ENABLED_BUCKET",
    "Rules": [
        {
            "Status": "Enabled",
            "Priority": 1,
            "DeleteMarkerReplication": { "Status": "Disabled" },
            "Filter" : { "Prefix": "PREFIX"},
            "Destination": {
                "Bucket": "arn:aws:s3:::DESTINATION_BUCKET"
            }
        }
    ]
}
